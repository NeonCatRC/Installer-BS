# Build output (packages are artifacts, never committed)
*.bs
*.sha256
*.sig
/build/
/dist/

# Transient extraction / packing
*.tmp
/tmp/

# Editor / OS noise
*~
.DS_Store
Thumbs.db
/article-draft-installer-sh.md

# Agent-local settings (a shared .claude/settings.json may be committed; local overrides are not)
.claude/settings.local.json