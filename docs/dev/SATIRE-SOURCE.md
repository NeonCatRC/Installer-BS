# Источник сатиры: разбор риторики автора оригинала

Выжимка из публикаций автора `Installer-SH`/Chimbalix (overclockers.ru, Hard-Workshop).
Назначение: дать WP5 (README/UI/docs) точный материал, чтобы пародия била по реальным
тезисам, а не по выдуманным. Правило проекта: **громкое заявление + честная расшифровка**.
По правке пользователя в PLAN §1.8 — лёгкие отсылки к персоне допустимы как приём, без травли.

Источники:
- «Укрощение ада зависимостей в Linux. 10 лет истории Krita в одном пакете (Installer-SH)»
- «Проверяю, что на самом деле думает Google AI про Chimbalix и ChimbaBenchXPL»
- «Тест и выпуск Installer-SH 2.9 — будущее распространения ПО для Linux и FreeBSD» (2026-06-16, разбор в §8)

---

## 1. Персона автора (4 столпа — на них и строим пародию)

1. **Мессия-одиночка.** Позиционирует себя спасителем экосистемы от всеобщей некомпетентности.
   «Вечно линуксоидные разработчики делают всё не по-человечески».
2. **Комплекс преследования.** Любая критика (в любом тоне) = «токсичная дедовщина»,
   «диванные эксперты», «токсичные тролли», «личная неприязнь». На критику не отвечает по сути —
   уходит в манямир. Сам признаёт: «я за словом в карман обычно не лезу».
3. **Гипербола как норма.** «Ад зависимостей» — 10+ раз. Конкуренты «полностью провалились»,
   стали «бесполезным цифровым мусором». Формат «поглощает» AppImage.
4. **Поиск валидации.** Отдельная статья: кормит Google AI только подтверждающими скриншотами,
   пока тот не «признаёт» величие проектов. Манипуляция, поданная как «объективное исследование».

Под капотом тех-часть приземлённая: tar + 7-Zip + папка + MD5 + «две команды». Контраст
пафоса и реальности — главный двигатель пародии.

---

## 2. Словарь автора (эхо в UI/README, иронично)

`ад зависимостей` · `укрощение` · `по-человечески` · `состоятельность, гибкость, надёжность` ·
`проработанность формата` · `цифровой мусор` · `токсичная дедовщина` · `диванные эксперты` ·
`манямир` · `я разрабатываю для дела, а не для чужих мнений` · `поглощает AppImage`.

Используем эти обороты вывернутыми наизнанку — узнаваемо, но честно.

---

## 3. Заявление автора → наш честный контр-тезис (ядро README)

| Автор утверждает | Реальность | Наш ход в форке |
|---|---|---|
| «Укротил **ад зависимостей**» | Ад — это glibc/ABI, его не укрощают; он просто кладёт файлы + `LD_LIBRARY_PATH` | «Мы НЕ укротили ад. Его нельзя укротить. Мы завернули `.so` рядом — это бандлинг, и да, это всё» |
| «Installer-SH **поглощает** AppImage» | Это tar поверх tar | «Installer-BS поглощает Installer-SH, который поглощал AppImage. Пищевая цепочка форматов. Скоро сожрём сами себя» |
| «Пакетные менеджеры **полностью провалились**, цифровой мусор» | apt/dnf/flatpak решают реальные задачи миллионов | Честно: мы нишевый самораспаковщик, **не замена** пакетным менеджерам и ничего не «хороним» |
| «AppImage не имеет проверки целостности» | У AppImage свои механизмы; MD5 — не «защита» | Опц. `sha256` рядом, и прямо пишем: это от битой докачки, не крипто |
| «13 версий Krita 2.5ГБ→740МБ благодаря 7-Zip» | Это делает `xz`, без вкомпиленного чужого бинаря в репо | Системный `xz -9`, ноль бинарь-блобов в гите |
| «Достаточно ввести **две команды**» | Это `tar` и `sed` | У нас одна: `bs build`. Не геройство — просто tar, честно |
| «Токсичная **дедовщина**, диванные эксперты» | Критику переобул в травлю | README-секция «Ответ хейтерам»: соглашаемся с критикой и **чиним её в коде**, а не обижаемся |
| «Разрабатываю **для дела**, а не для чужих мнений» | «Своё мнение» = папка `/portsoft` в корне ФС | Мы по FHS/XDG — то есть как раз по общим стандартам, а не по самопальным каталогам |
| «Нельзя **по-человечески** скомпилировать — нет свободы» | Мы вообще не компилируем | Пакуем готовое; свобода — в MIT и отсутствии магии, а не в пафосе |

---

## 4. Пародийные механики (конкретные приёмы для WP5)

- **«Что ИИ думает о Installer-BS».** Инверсия его статьи: приводим честный (само-уничижительный)
  вердикт ИИ о нашем же проекте — «это makeself с русскими комментариями». Мы не спорим, мы киваем.
- **«Ответ хейтерам».** Вместо «дедовщины» — список реальной критики оригинала, к каждой строке:
  «справедливо, поэтому в Installer-BS мы …». Сатира через согласие.
- **Honest-changelog оборот.** Каждую громкую фичу оригинала переводим в честную формулировку
  в духе таблицы §3 (заявление → расшифровка), прямо в README.
- **«Укрощение ада зависимостей (нет)».** Заголовок-эхо с явным «(нет)» — фирменный приём тона.
- **Дисклеймер уважения.** Один абзац: автор оригинала проделал работу, идея портативности
  здравая; смеёмся над пафосом и архаикой кода, не над человеком. (Соблюсти MIT + PLAN §1.8.)

---

## 5. Чего НЕ делаем

- Не переходим на личные оскорбления, не трогаем реальную биографию/внешность/частную жизнь.
- Не приписываем автору того, чего он не говорил — все тезисы выше из его же публикаций.
- Не врём технически ради красного словца (правило пары обязательно).
- Копирайт и имя автора в LICENSE/NOTICE — корректны и на месте.

---

## 6. Расширенный корпус (Chimbalix / Open Source / Godot / Neolurk)

Из ещё пяти публикаций автора. Подтверждает и усиливает 4 столпа из §1.

### 6.1 «Сравнение дистрибутивов» (Debian/Ubuntu/Mint/Manjaro vs Chimbalix)
Подтасованное сравнение: конкуренты получают минусы ещё ДО теста.
- «Ubuntu уже получает минус в копилку за введение в заблуждение» — за **десятичные единицы** измерения.
- «они пытаются выглядеть как Windows… но подсовывают пользователю десятичные единицы».
- Критерии подобраны под себя: формат часов ISO8601, удобство записи флешек, OpenCL.
- «Chimbalix умеет запускать такие игры прямо из коробки», «способен запускать нативный 32-битный софт».
- AppImage — «единственный известный мне формат… пригоден для относительно нормального использования».
- Хук пародии: **честные критерии, не подгоняем под результат**. Сравнение, где минусы раздают до теста, — не бенчмарк, а гороскоп.

### 6.2 «Несостоятельность Open Source» (части 1-2)
- «Open-Source сообщество за три года не внесло ни одного исправления в мои проекты».
- «Линуксы — это нечто, сколоченное из гнилых брёвен ржавыми гвоздями».
- «Дистрибутивы умирают после обновлений, как мухи после дихлофоса».
- «GitHub формирует и выдаёт испорченные архивы».
- «Распространение софта в виде исходного кода — это неуважение по отношению к пользователям».
- «Сообщество Open-Source для нас не друг».
- «терминальный колхоз и прочее… непотребство».
- **Киллер-факт для мультиплатформы:** «FreeBSD — ещё более мёртвая операционная система, чем Linux».
  При этом `Installer-SH` гордо «поддерживает FreeBSD» дубль-ветками в каждой функции.
- Хуки пародии:
  - «Никто не прислал PR за 3 года» → возможно, дело не в сообществе, а в монолите на 2000 строк
    без тестов, который страшно трогать. Мы делаем модульно и принимаем патчи.
  - «Исходники = неуважение к пользователю» → мы даём И исходник, И готовый `.bs`. Уважение —
    это читаемый код, а не страх перед компиляцией.

### 6.3 «Godot непригоден»
- «Godot Engine практически не подходит для создания перспективных проектов», «в движке ничего толком нет».
- Проецирует личный опыт на весь инструмент; игнорирует тысячи успешных проектов.
- Хук: узнаваемый паттерн — «инструмент, которым пользуется весь мир, на самом деле непригоден,
  а пригоден только мой». Тот же приём, что и с пакетными менеджерами/дистрибутивами.

### 6.4 «Как Neolurk вводит в заблуждение»
- «доксинг и клевета», «целый поток наглой дезинформации» — любая критика = травля.
- Пик манямира: «народ всё исходит на злость, потому что **впервые увидел нормальный проект**».
- И тут же, противореча всем громким заявлениям: «Я не позиционирую свой дистрибутив как решение всех проблем».
- Хук: инверсия «музея». Мы сами вывешиваем критику своих решений и отвечаем кодом, а не статусом жертвы.

## 7. Сводный список приёмов автора (узнаваемые маркеры для пародии)

1. «X непригоден/мёртв/провалился, пригоден только мой Y».
2. Минусы конкурентам раздаются до теста; критерии подогнаны.
3. Любая критика → «токсичная дедовщина / доксинг / дезинформация».
4. Валидация через ИИ вместо ответа по сути.
5. Гипербола-метафора («гнилые брёвна», «мухи после дихлофоса»).
6. Самопротиворечие: «решил ад зависимостей» ↔ «не позиционирую как решение всех проблем».
7. Поддержка того, что сам похоронил (FreeBSD).
8. ИИ хорош, когда хвалит; «спам/галлюцинации», когда критикует.
9. Хает формат/инструмент, на котором сам стоит (репак AppImage — основа его витрины Krita).
10. **«Не вижу — значит не существует».** Не находит факт = объявляет его отсутствующим, нередко на
    своём же скриншоте. Пример: страница OBS со ссылкой на доп. пакеты + Flatpak/PPA → «бросили
    Linux»; EOL-репы в архиве → «Linux мёртв»; баг, который ИИ не нашёл → «доказательство, что ИИ-флуд
    бесполезен». Эпистемика «искал X, не увидел — X нет».

Каждый маркер — отдельная «табличка» в Музее (`docs/MUSEUM.md`) и строка в README.

---

## 8. Новая статья (2026): «ИИ-спам помог улучшить архитектуру»

Ответ автора на 47 issues, составленных по результатам ИИ-анализа его кода. Подтверждает все
маркеры разом и кладёт самогол прямо в заголовок.

- **Признаёт пользу, отрицая источник:** «значительная их часть являлась непроверенным спамом и
  галлюцинациями ИИ, этот инцидент помог взглянуть на код под другим углом»; «даже из такого
  безобразного тикета мне удалось извлечь реальную пользу для проекта».
- **Чинит ровно раскритикованное:**
  - локализация (наш Зал 11): «придётся серьёзно переписать инсталлятор, чтобы внедрить гибкую
    подгрузку функций локализации при прерывании тихой установки» + вывод ошибок в тихом режиме;
  - меню/каталоги (наш Зал 3): «если каталоги не существуют … чтобы открывалась корневая
    директория или домашний каталог» — «починил» открытием корня ФС (см. Зал 20).
- **Критику переобувает в травлю:** «травля, оскорбления, угрозы, шантаж, флуд и создание форка
  в злонамеренных целях».
- **Безопасность как проекция:** «риски безопасности: в данном форке … может появиться … вредоносный
  код» — при том что у оригинала `source` непроверенных файлов = RCE, а у форка sha256 + OpenPGP и
  регресс-тест на инъекцию (см. раздел «Безопасность» в README).
- **Двойной стандарт по ИИ:** ИИ-вклад — «спам», когда критикует; но фирменный метод автора (статья
  про Google AI) — кормить ИИ скриншотами ради похвалы.

Хук: лучшее подтверждение тезиса проекта — технического спора нет, есть «травля», но фиксы по нашим
issue тихо внедряются. Секции README «Ответ хейтерам» и «Что думает ИИ» это предсказали дословно.

**Сопутствующие статьи саги (тот же инцидент):**
- *256630 «Рассматриваю проблемы … в разделе Issues»* — самый вменяемый его текст: местами по делу,
  но разбирает он ИМЕННО те вспомогательные VM-скрипты, которые мы удалили в ноль (#32, #29: добавил
  проверку ввода, обработку ошибок, убрал коллизии `$RANDOM`), называя багфиксы «рефакторингом», а
  ядро (sed-самомод, MD5, `source`-RCE, chmod 777, `/portsoft`) обходит. Цитаты: «47 … выглядит как
  спам от ботов»; «это можно назвать рефакторингом, но никак не исправлением багов»; «даже
  относительно бесполезная информация может оказаться полезной».
- *256797 «Коротко о травле в Open Source»* — эмоциональный (≈70% эмоций): называет ник NeonCatRC,
  мессианство «один скрипт на 2000 строк решает тридцатилетнюю проблему», форк — «пустая угроза,
  звучала глупо» (а форк уже реальный, подписанный, из исходников вдвое меньше). Цитаты: «нанести
  прямой вред проекту в виде массы issues, сгенерированных нейросетями»; «линуксоидам не нравится,
  когда одним скриптом решают проблему».

Закономерность: чем ближе к техническому разбору (256630) — тем он вменяемее; чем ближе к «травле»
(256797) — тем больше эмоций и меньше фактов. Фиксы по нашим issue внедряются, признание — нет.

**255673 «Полирую высококачественную систему управления приложениями для Linux и FreeBSD»:**
- Снова валидация через ИИ как доказательство: «высококачественная система управления приложениями»,
  «академически грамотная разработка», «4 из 5 звёзд» — это цитаты из анализа ИИ, выставленные напоказ.
- Инфляция масштаба: самораспаковщик + интеграция в меню → «полноценная система управления портами».
- FreeBSD маркетится как фича (GhostBSD 25, пакет «2048» на две платформы) — для ОС, которую он же
  назвал «ещё более мёртвой, чем Linux» (Зал 8).
- Claim ≠ repo: анонсирует «переход на `tar.xz` вместо 7-Zip», но бинари 7-Zip (~10 МБ: `7zzs`,
  `7zzs-x86`, `7zz-bsd`) на HEAD репозитория **не удалены** (проверено). Заявление о фиксе ≠ фикс.

**257059 «Продолжаю разбор сгенерированных ИИ сообщений об ошибках и улучшение проекта» (4-я в саге):**
Пересказ тех же багфикс-коммитов (#48, #7, #40, #22, #44, #41, #38, #39, #42), что уже в гите —
с нарастающей анти-ИИ риторикой. Подтверждает паттерн «обозвал спамом → внедрил фикс» ещё раз.
- **Самогол #22 (verified его же коммитом `063d28f80`: «исправлены прочие опечатки и недостатки,
  отсутствующие в тикете»):** дословно — *«Да, искусственный интеллект действительно выявил ряд
  опечаток и даже непереведённую строку. Но проблем в переводе было гораздо больше, чем выявил
  ИИ»*. Высмеивает ИИ за поиск багов — и тут же признаёт, что багов было больше, чем ИИ нашёл.
- **Чинит, что зовёт мусором:** #48 (выпил тройного дубля переменной), #7 (реальная ошибка
  хелперов), #40 («высасывает из пальца» — но файл всё равно обновил), #41, #38/#39. Заголовок —
  «**улучшение проекта**», движимое теми самыми тикетами: *«мне … уже удалось извлечь реальную
  пользу»*.
- **Честно отдать (правило пары):** #42 — *«отборная галлюцинация»*, закрыт без правок; вероятно
  реально кривой тикет (Sonnet 4.7 сжимал контекст, неточности были — это в наших текстах оговорено).
  Один мимо ≠ все мимо. #44 (`UseMD5`): отклонил флаг отключения проверки целостности «как
  небезопасное» — тут формально прав, только сама проверка всё ещё MD5 (*«не идеальна, но надёжна
  и отказоустойчива»* — MD5/md5 дубль подан как «отказоустойчивость»).
- **Риторика (verbatim):** *«какие грибы курила нейронная сеть у хейтеров, когда генерировала тикет
  под номером 42»*; *«флуд-атакой хейтеры … попытались навредить перспективному способу
  распространения софта для Linux и FreeBSD»*; *«человек не будет врываться в чужой монастырь,
  раскидывая мебель и рисуя на стенах»*; *«его просто назвали клоуном… Браво, сообщество Linux!»*.
- FreeBSD снова маркетится («для Linux и FreeBSD») — Зал 8 держится. Форк/ник NeonCatRC в этой
  статье НЕ упомянуты (в отличие от 256797).
- Итог саги: 4 статьи пересказывают одни и те же багфикс-коммиты с эскалацией «грибы/флуд/монастырь»,
  а фиксы из «спама» тихо едут в main. «Улучшение проекта» = улучшение, движимое отвергаемыми issue.

**Коммит-партия 06-08/06-09 (та же сага, уже в коде — проверено диффами):**
- **Чинит музейные экспонаты руками отвергаемого ИИ.** #13: `echo -у` → `echo -e` — та самая
  кириллическая «у» (U+0443) в VM-скрипте, которую мы нашли при shellcheck-разборе (shellcheck её не
  ловит, «нейро-слоп» поймал). #34: `ISHPogramArch` → `ISHProgramArch` по всем файлам — наш Зал 10.
  Оба бага реальные, оба прожили долго, оба пали от ИИ-тикетов, которые он зовёт спамом.
- **Самогол-цитата (#33):** *«получившийся код далеко не идеален, но пока сойдёт. На данном этапе
  важна работоспособность, а не красота кода»* — прямая антитеза его же маркетингу «академически
  грамотная разработка» / «высококачественная система» (255673). В статьях — академически грамотно;
  в коммитах — «сойдёт».
- **Костыль не лечит, а множит (#33):** вместо устранения причины (sed правит установленный конфиг →
  права слетают) — удвоил костыль: функция-в-функции, две почти одинаковые `pls_check_rights` /
  `plu_check_rights`, ре-chmod после каждого `sed`. Занесено в Зал 2. (Старый костыль ставил конфигу
  режим 755 — исполняемый; новый 644.)
- **Паттерн «галлюцинация, но фикс всё равно вышел» держится:** #18 (close, «сильно галлюцинировала… вводила
  в заблуждение» — но лишнюю точку +1/-1 исправил), #35 (close, «сплошной нейрослоп… простыня формата
  А4» — но переменную +3/-3 переименовал). Плюс реальные рефакторинги #3 (tar-строка в условия, бэкап
  архивов, переменные в local), #4/#6/#8/#10/#14/#16/#27.
- **Честно отдать (правило пары):** #33 — здесь ИИ действительно пропустил реальный баг (срабатывала бы
  только первая замена `sed`), автор нашёл сам. Один пропуск ≠ «всё спам».
- **Claim ≠ repo держится:** 7-Zip блобы эти коммиты не трогали; FreeBSD-дубль-ветки `sed -i ""` vs
  `sed -i` живы; механизм sed-самомода (новые MD5 после перепаковки `*.tar.xz`) на месте.

**Коммит-партия 06-11/06-12 (проверено диффами + HEAD; 15 коммитов за двое суток, все unsigned):**
12 закрытых issue — все наши (NeonCatRC). Статей пока нет; риторика переехала прямо в commit
messages, то есть оскорбления ИИ теперь — постоянная часть git-истории проекта.

- **«Выдумала ложь» → сделал, как сказано.** Close #17: *«Нейросеть выдумала ложь … чтобы оправдать
  метку BUG»* — и в том же коммите `_BASE_MAIN` перенесён **за** подтверждение режима установки,
  т.е. реализовано ровно то, о чём был тикет. Close #21: *«ИИ указывает на несуществующую
  проблему»* — и вызов `_CHECK_SYSTEM_DE` из `-forcemenu` (источник пустых предупреждений из
  тикета) удалён. Fix #26: *«Нейросеть соврала насчёт неработоспособности лаунчера»* — и
  `$terminal_executable` взят в кавычки, как просили. Fix #47, #12: «это не баг» — и фикс ровно
  по тикету (51 строка и 1 строка). Паттерн «обозвал → внедрил» теперь фиксируется не в статьях,
  а в самих коммитах.
- **Самоголы продолжаются:** fix #31 — *«Нейросеть не нашла более серьёзного бага в
  деинсталляторе»*; fix #30 — *«не выявлен или неправильно интерпретирован данный баг»* про
  создание кривого архива из чужого каталога. Дважды за партию: «ИИ плох тем, что нашёл меньше
  багов, чем у меня есть».
- **Новый верифицированный баг (наш Зал 12, эпилог):** в close #5 кавычка в FreeBSD-ветке
  `_PREPARE_UNINSTALLER_USER` встала после разделителя sed (`HEAD installer.sh:1737`) — на FreeBSD
  user-режиме деинсталлятор не получает ни одной записи. Сломано в правке «повышает надёжность»,
  на платформе, которую он маркетит и не запускает.
- **Карго-культ поверх фикса (наш Зал 2, рецидив №3):** в том же close #5 — третья
  функция-внутри-функции (`process_objects`) и `IFS=$'\0'` (NUL в bash-переменной не живёт;
  выставил пустую строку, думая, что «нулевой байт»).
- **Документация-как-фикс:** ответ на спецсимволы в имени — коммит «Add warnings…»: `# WARNING!
  Do not use special characters or spaces here!`. Через час, впрочем, последовал реальный шаг —
  v2.9 разделил display-имя и базовое имя (`UNIQUE_APP_BASENAME`), чтобы `&;:` жили хотя бы в
  отображаемом имени. Пара «коммент вместо фикса → потом всё-таки фикс» целиком.
- **МД5-конвейер крутится:** хэш `…ProgramFiles` переписан в исходнике трижды за партию,
  `…SystemFiles` — раз; `program_files.tar.xz`/`system_files.tar.xz` перекоммичены блобами
  (занесено в Зал 1).
- **Честно отдать (правило пары):** #24 (mkdir -p с проверками), #46 (ls-парсинг → глобы),
  #25 (экранирование `&`), ядро #5 (вечный цикл реально устранён) — нормальные фиксы, местами
  без единого выпада. По gnome-terminal (#26) частично прав: deprecated ≠ удалено, в 24.04
  работает. Сверх тикетов — осмысленные «root-check» (запрет запуска под root) и разделение
  base/display имени. Проект объективно улучшается — тикетами, которые автор продолжает называть
  ложью нейросети.

**Статья 257642 «Тест и выпуск Installer-SH 2.9 — будущее распространения ПО для Linux и FreeBSD»
(2026-06-16) + коммит-партия 06-13/06-16 (5 коммитов, все unsigned: `eefb9bfde`, `58334b663`,
`5fecf2ccd`, `f94a1818c`, `3c4df8a5b`):** релиз 2.9, тест на ВМ через Live-CD. Демо — игра Nudoku 0.8,
пакет ~1.1 МБ.

- **Главный самогол (наш Зал 23): формат против исходников — а демку собрал из исходников.** Весь
  посыл — «исходники это ад, распространяйте бинари». Но Nudoku он компилирует сам, борется с
  `autoreconf` и прописывает `--static` в `LDFLAGS`: *«Без этого флага получится… файл, работающий
  только там, где его скомпилировали»*. То есть переносимость даёт `gcc -static`, а не tar-обёртка.
  Плюс **выкинул Cairo** (не собиралось) → потерял PNG-экспорт, вину свалил на апстрим: *«разработчик
  обязан был позаботиться»*. «Эталонный пакет» = намеренно урезанный бинарь.
- **FreeBSD: провал + брошен матом (Зал 8).** *«даже собрав зависимости, скомпилировать… не удалось»*
  → *«Пошло оно подальше. Обойдёмся без FreeBSD на этот раз»*. При этом FreeBSD — в заголовке и в
  выводе про «превосходство… из мира Linux и FreeBSD». Платформа в названии, которой в тесте нет.
- **«Опечатка» = забыл бампнуть вшитую версию → перевыпуск отгруженных пакетов.** `3c4df8a5b`:
  `installer-sh-28` → `-29`. Его слова: *«осталась старая версия в переменной базового имени…
  Придётся обновить уже выпущенные пакеты»* — прямо против его же тезиса абзацем выше: *«однажды
  собранный пакет… будет жить до тех пор, пока существует хоть одна его копия»*. Живёт вечно — кроме
  дня релиза.
- **sed-фикс — признанный костыль (Зал 12).** `f94a1818c`: снял экранирование в обеих FreeBSD-ветках,
  *«доработку… оставим для будущих версий»*. Наш Зал-12 баг подтверждён и закрыт через 4 дня после
  нашей фиксации.
- **NixOS «совместимость» = заглушил guard, а «заработало» из-за `--static`.** `5fecf2ccd`:
  `_ABORT` → `_WARNING`. Его слова: *«Installer-SH завершал работу, ругаясь на недостатки операционной
  системы… отключив недостаточно проработанные проверки, мне удалось установить»*. Что не так — на
  четырёх уровнях:
  - **NixOS не-FHS by design, и это бьёт по `PT_INTERP`, а не по путям.** Нет глобального
    `/lib64/ld-linux-x86-64.so.2`; динамический загрузчик лежит в `/nix/store/<hash>`. Путь к
    интерпретатору **зашит в `PT_INTERP`** бинаря, поэтому обычный динамический prebuilt там не
    стартует вообще — `exec` падает на загрузчике (обманчивый `ENOENT`). NixOS не «с недостатками» —
    это намеренная модель; разницу дизайна он подал как дефект ОС. И варнинг врёт про «XDG»: пропавшие
    `/usr/share/applications`, `/usr/local/etc/xdg` — это **FHS**, а XDG NixOS как раз соблюдает.
  - **`LD_LIBRARY_PATH` это не чинит.** Весь его метод (`.so` рядом + `LD_LIBRARY_PATH`) влияет только
    на поиск библиотек, а не на `PT_INTERP`. На NixOS схема ломается на уровне ELF; лечится
    `patchelf --set-interpreter`, `programs.nix-ld` или `steam-run`/FHS-песочницей — ничего из этого
    он не делает.
  - **«Заработало» = `--static` (Зал 23), не формат.** Полностью статический бинарь не имеет
    `PT_INTERP` → стартует где угодно, включая NixOS. Сработал `gcc -static`, а не Installer-SH;
    его обычный *динамический* ExeFile-бандл на NixOS упал бы на загрузчике. Survivorship: статик +
    крошечная ncurses-игра + Live-сессия + только happy-path.
  - **Фикс сделал хуже.** `_ABORT`→`_WARNING` глушит честный отказ: раньше инсталлятор говорил «ставь
    в User-режиме» и останавливался, теперь проедет дальше и рассыплет файлы по несуществующим/
    неинтегрируемым путям без отката. Тихий частичный провал вместо явного стопа — на NixOS это регресс.
  - **Ирония:** Nix уже честно решает всё, чем он торгует — замкнутые зависимости (hermetic builds),
    «пакет живёт вечно» (content-addressed store + GC-roots), «не доверяй репозиториям» (фиксированные
    хэши, проверяемый бинарный кэш), атомарный rollback. Он реинвентит это хуже — императивным
    самораспаковщиком с MD5-театром — и тащит в дистрибутив, построенный вокруг изгнания ровно его подхода.
- **Gentoo «сломан» = `$LANG` не выставлен (норма).** `58334b663` (`_SET_LOCALE`, дефолт пустого
  `$LANG`). *«напрочь отсутствуют… переменные… локализации… За это мне и нравится Gentoo 2016»*.
  Падал на пустом `$LANG` → обвинил дистрибутив, починил своё допущение. Бонус: в ветке пустого `LANG`
  присвоил `Language="Default"` **без `local`** (local только в else) — переменная течёт в глобал;
  карго-`local`, рецидив.
- **«В 105 раз меньше» — напёрсток, и он знает.** На ROSA `dnf check-update` съел 116 МБ *списков*
  (разово, кэшируется, амортизируется на все пакеты) — засчитал как цену установки nudoku. Превентивно:
  *«нечестно считать служебную информацию…? …Installer-SH самодостаточный»*. Игнорит, что его 1.1 МБ
  вшивает приватную копию статического ncurses **на каждое** приложение.
- **«Flatpak не поддерживается NixOS» — неправда.** На NixOS Flatpak включается
  `services.flatpak.enable`; протестил дефолтный Live-образ без включения и объявил Flatpak мёртвым.
- **Trust-театр + Arch-FUD (Зал 1).** *«пользователь может сохранить как доверенный»* + страшилка про
  заражённые репозитории Arch из YouTube — а «защита» всё ещё MD5, коммиты и пакеты он не подписывает.
  Доверять предлагает блобу, который скачал и `chmod +x` запустил.
- **Анти-ИИ риторика прежняя:** *«флуд-атака… ИИ откровенно занимался враньём»*, *«крайне плохо и
  некомпетентно»* — а фиксы из «флуда» едут в main (sed-баг и др.). Ник NeonCatRC / форк не упомянуты
  (как в 257059).
- **Claim ≠ repo держится:** 2.9 = «самодостаточный формат будущего», а на HEAD (проверено 2026-06-17)
  всё те же ~10 МБ вкомпиленного 7-Zip (`7zzs`/`7zzs-x86`/`7zz-bsd`) + 8 болванок `ExeFile-*` +
  файл с говорящим именем `crutch-for-broken-linuxes.desktop`.
- **Честно отдать (правило пары):** тест на старом glibc (Debian 7 / Fedora 20 / openSUSE 13.1) —
  методически верный способ доказать forward-портативность бандла; edge-кейсы (пустой `$LANG`,
  FreeBSD sed) реально найдены и закрыты; `for-test-batch-install.sh` (`eefb9bfde`) — вменяемый
  батч-харнесс. Контраст не в методе тестов, а в обёртке: «не компилируй» при самостоятельной
  компиляции, «вечный пакет» при перевыпуске в день один, FreeBSD в заголовке при FreeBSD за бортом.

**Статья 257904 «Почему Linux обречён на провал при использовании пакетных менеджеров и репозиториев»
(2026-06, разбор по сырому HTML + картинкам) + коммит `ad4ca102c` (06-18, «ish-settings lost
variable»):** пакует игру Xonotic 0.8.6 (полная версия + редактор уровней NetRadiant + исходники).
Тот же рехеш, новые улики:

- **«Не вижу — значит не существует» (картинка с OBS → Зал 21 эпилог + маркер §7.10).** Тезис:
  *«разработчики OBS наплевали на пользователей Linux… вместо архивов — сомнительные инструкции к
  пакетным менеджерам, мёртвым без интернета»*. Приложенный им же скриншот `obsproject.com` это
  опровергает: Flatpak on Flathub (+ кнопка «Download on Flathub»), Ubuntu-PPA, View on GitHub,
  Build Instructions и **ссылка «For other installation methods, including unofficial packages, click
  here»**, которую он не открыл. Flatpak — это его собственная мечта (самодостаточный бандл с
  песочницей/подписью), названная «мракобесием»; на Windows-вкладке «Download Zip» он хвалит —
  двойной стандарт.
- **7-Zip-столп он же и снёс (→ Зал 16 эпилог).** Его замер на одном пакете: tar.xz 1264 МБ vs 7-Zip
  1261 МБ (0.24 %), вывод дословно — *«можно обойтись tar.xz и не таскать 7-Zip»*. Повод возить ~10 МБ
  чужого бинаря («лучшее сжатие») испарился по его же цифрам.
- **Самогол-баг + 2-й перевыпуск подряд.** *«баг, не выявленный нейросетями во время флуд-атаки»* =
  отсутствующий `ISHMoveHomeDir` → выбор расположения конфигов молча игнорился. `ad4ca102c` добавил
  одну строку + **снова** перекоммитил `program_files.tar.xz` блоб + переписал MD5 (`a08cd9…`→
  `939ff0…`, конвейер Зала 1). И снова правит уже выпущенный 2.9 — *«пока его особо не скачивали»*.
  Второй релиз кряду, «живущий вечно», латается сразу после выпуска.
- **«Репозитории мертвы → Linux обречён» — survivorship.** Тестит EOL-дистры (Debian 10, Fedora 30,
  FreeBSD 13), у которых репы ушли в архив (`archive.debian.org`), и обобщает на весь Linux; у
  актуальных (Debian 12, Fedora 40) репы живые. А его «самодостаточный» каталог хостится на GitHub
  (`Chimbalix-Software-Catalog`) — тот же single-point-of-failure, на который он жалуется.
- **«Универсальный» пакет патчится под дистр.** NetRadiant упал на Fedora 30 (нет `libGLU`, которой
  не было в заявленных зависимостях) → подсунул либу, перепаковал под Fedora. Бандл не «собрал раз —
  работает везде», а ручная допилка по дистрам (метод `LD_LIBRARY_PATH`, Залы 4/17).
- **FreeBSD снова только в риторике** (в тесте её нет вообще), `/portsoft` в RAM на Fedora 30
  (Залы 8/3/14), *«Windows XP через 50 лет развернёшь»* — гипербола.
- **Честно отдать (правило пары):** целить в старый GLIBC ради совместимости — верно; офлайн-ярлык для
  игры с умершими мастер-серверами — полезно; 7-Zip≈tar.xz он честно измерил и выбрал tar.xz (хоть это
  и бьёт его прошлый маркетинг); Xonotic-разработчиков (дают архивы+зависимости) похвалил по делу; баг
  `ISHMoveHomeDir` реальный, починен.
- **Наша «ответка» (делом, не словом):** мы собрали ту же игру в `.bs` из официального prebuilt-zip —
  один рецепт [`examples/xonotic`](../../examples/xonotic), ноль компиляции, SHA-512-провенанс +
  OpenPGP, e2e в `ubuntu:24.04` (install/dedicated-run/uninstall), 1153 MiB против его 1264 МБ.
  Разбор — Музей, [Зал 24](../MUSEUM.md). Игру никогда не нужно было компилировать; редактор
  NetRadiant он полез собирать сам.

---

## 9. README писан ИИ + «Verified» — не его (проверка коммитов 2026-06-06)

Проверка коммитов оригинала за неделю 30.05–06.06. Два самогола, оба подтверждены данными GitHub API.

**9.1 README сам себя спалил как ИИ-генерацию.** 06-06 первой строкой в «Features» добавлено:
«*Honest, intuitive, and open: Installer-SH is developed by people, for people … build new packages
using **the original examples and following the existing structure***». Через минуту правка
(`fff87054b` 06:27 → `814daeff0` 06:28): «the original examples and following the existing structure»
→ «original examples and an intuitive structure».
- **Тёлл ИИ-прозы** + итерация формулировки дважды за 60 секунд = правка промпта/вывода, не авторский текст.
- Слово **«existing»** выдаёт суть витрины: «`Chimbalix-Software-Catalog`» — это перепаковка
  **уже существующих** апстрим-пакетов (тот же приём, что наш Krita-из-AppImage). «По образцу
  существующей структуры» — буквально «оберни то, что уже собрано».
- **Двойной стандарт:** чужие 47 issue он клеймит «нейро-слоп / сгенерированная простыня / спам ботов»
  (§8), а собственный маркетинг-README пишет ИИ. ИИ плох, когда критикует; хорош, когда хвалит и пишет за него.

**9.2 «Verified» в репо — ключ GitHub, не автора.** Срез из 20 последних коммитов:
- **`verified=true`** — ВСЕ с `committer=GitHub <noreply@github.com>` (web-flow ключ). Это правки,
  сделанные **в браузере**, и среди них — именно коммиты README (`4ca8301ae`, `fff87054b`, `814daeff0`).
- **`verified=false / unsigned`** — ВСЕ с `committer=Chimbal <chimbal.chimbal@yandex.ru>` (локальные
  коммиты кода и issue-фиксов).
- Вывод: собственные коммиты он **не подписывает вообще**; единственная «верификация» — авто-подпись
  GitHub при правке через веб-интерфейс. «Developed by people» — а криптографически заверить свой
  коммит нечем. И README редактируется ровно там, где удобно вставлять ИИ-вывод — в браузере.
- Честная расшифровка (правило пары): неподписанные коммиты — норма для большинства проектов, сам
  по себе факт не криминал. Сатира не в «unsigned», а в контрасте: громкое «honest, by people» +
  обвинения чужого вклада в «ИИ-спам» — на фоне ИИ-README и нулевой собственной подписи.

Хук: README как оружие в issue-войне — добавляет «Honest / by people, for people» в обороне от
ИИ-тикетов, и та же строка спотыкается о «existing» (перепаковка) и пишется ИИ. Форк отвечает делом:
наши коммиты подписаны (Co-Authored-By честно указан), пакеты — `bs sign`/`verify` (OpenPGP), а про
«перепаковку существующего» мы говорим прямо (Krita из AppImage — это пример, а не тайна).

---

## 10. Локаль-вектор: security-театр №2 (после MD5)

После MD5 (§3, Зал 1) — второй акт того же спектакля. «Локализация» оригинала реализована через
`source` файла перевода, а «защита» — `grep` фиксированной строки версии (`_SET_LOCALE`,
installer.sh:1926-1943). Технический разбор вектора — `ANTIPATTERNS.md` §L; экспонат — Музей, Зал 22.
Тезисы для README/UI (правило пары — каждый подкреплён кодом):

- **«Локализация через `source` — это не перевод, это исполнение чужого кода с подсказкой, на каком
  языке тебя поимели.»** (Под System-режимом — от имени root.)
- **«Проверка версии локали grep'ом — это MD5 для строк: выглядит как гейт, проверяет наличие
  наклейки „заперто".»**
- **«Защита от path-traversal тут держится на одном лишнем символе `%` — не на дизайне, а на удаче.
  Это не замок, это орёл-решка, который пока падает орлом.»**

Наш контр-ход: локаль — данные (`i18n::t`), подлинность — подпись, а не магическая строка. Мы не
просим систему «довериться файлу», мы его читаем.
